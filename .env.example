# OpenPX 不再从 .env 读取模型配置。
# 请改用下面这些配置文件：
# - ~/.openpx/openpx.jsonc
# - <workspaceRoot>/openpx.jsonc
# - <workspaceRoot>/.openpx/settings.local.jsonc
#
# 如果需要通过本地代理访问模型，可在 shell 或 .env 中设置这些代理变量。
# 它们只影响网络连通性，不负责 provider/model 选择。

# http_proxy=http://127.0.0.1:7890
# https_proxy=http://127.0.0.1:7890
