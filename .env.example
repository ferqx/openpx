OPENAI_API_KEY=your-provider-key
OPENAI_BASE_URL=https://your-openai-compatible-endpoint
OPENAI_MODEL=kimi-k2.5

# 如果需要通过本地代理访问模型，请先确认代理端口已经可用，
# 再在 shell 中设置 http_proxy / https_proxy 后执行 smoke 或 dev。
